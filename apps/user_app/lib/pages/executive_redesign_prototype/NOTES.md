# Executive (ผู้บริหารสถานศึกษา / ผอ) Redesign Prototype

สถานะ: PROTOTYPE SANDBOX — mock data ทั้งหมด ยังไม่ผูก Supabase จริง
(บันทึกไว้ 2026-08-15 โดย Claude Code)

## ที่มา — ตรวจสอบวอลต์กลางก่อนเริ่มสร้าง

ต่างจากฝั่งผู้ดูแลอาคาร (`facility_redesign_prototype/`) ที่มี 7 ยูสเคสเฉพาะ
ทาง (STK-6..12) — ตรวจวอลต์กลาง (`AIoT-School-Lab-Vault`) แล้วพบว่า
**ผู้บริหารสถานศึกษา (ผอ) มียูสเคสเฉพาะทางแค่ตัวเดียว**:

- **LA-9** "ดูรายงานภาพรวมของสถานศึกษา" — Goal: เห็นภาพรวมทั้งโรงเรียน
  (การเรียนรู้ พลังงาน สิ่งแวดล้อม ความปลอดภัย การใช้ระบบ) ในหน้าเดียว
  กรองตามช่วงเวลา/ระดับชั้นได้ — **BR1 (สำคัญ): เห็นภาพรวมระดับโรงเรียน
  เท่านั้น ห้ามเห็นข้อมูลรายคนเชิงลึกที่กระทบความเป็นส่วนตัว**

สิทธิ์อื่นที่ ผอ มีเป็นสิทธิ์ที่ยืมมาจากยูสเคสของ role อื่น ไม่ใช่หน้าจอ
เฉพาะของตัวเอง:
- **LA-11** Export PDF/Excel (Primary Actor: ครู/ผู้บริหาร)
- **SEC-7** รายงานความปลอดภัยทั้งโรงเรียน (Primary Actor: ผู้บริหาร/School
  Admin — เป็นต้นทางที่ STK-10 ของผู้ดูแลอาคารเป็นแค่ตัวกรองย่อยระดับอาคาร)
- **EMG-4 (Exception Flow)** — ผอ เป็นผู้รับแจ้งเตือนฉุกเฉินตั้งแต่แรกร่วม
  กับครูประจำพื้นที่ และเป็น**เส้นทางยกระดับ**ถ้าไม่มีใครรับทราบทันเวลา
  (**ไม่ใช่ผู้ยืนยันปิดหลักตาม EMG-3 ซึ่งเป็นหน้าที่ของครูก่อน** — ผอ เป็น
  แค่ทางสำรอง)
- **SEC-5 (Exception Flow)** — ถ้าครูไม่แน่ใจว่า Alert จากกล้อง AI เป็นเหตุ
  จริงหรือ false positive จะถูกส่งต่อให้ School Admin/ผอ ช่วยตัดสินใจ

**ยังไม่ได้ทำ (เก็บไว้เป็นหน้า 2 ถ้าต้องการ)**: หน้า "ศูนย์แจ้งเตือน/เคสที่
ต้องตัดสินใจ" ครอบคลุม 2 จุดข้างบน (EMG-4 escalation + SEC-5 escalation) —
ยังไม่เริ่มสร้าง

## ✅ สร้างแล้ว (2026-08-15) — หน้า 1: Dashboard ภาพรวมสถานศึกษา (LA-9)

`executive_dashboard_page.dart` — หน้า standalone (มี Scaffold ของตัวเอง
ต่างจากฝั่งผู้ดูแลอาคารที่เป็น content-only ต่อกับ shell เพราะยังไม่มี
shell/nav ของ role นี้) ประกอบด้วย:
- Hero header ไล่เฉดสี indigo เข้ม (ธีมแยกจาก FacilityTheme ตั้งใจ ให้แต่ละ
  role มีธีมของตัวเอง) + ปุ่ม Export (LA-11) มุมขวา
- ตัวกรองช่วงเวลา/ระดับชั้น (ตาม LA-9 Main Flow ข้อ 3)
- 4 มิติ ตาม Main Flow ข้อ 2: ผลการเรียน (จาก LA-3), พลังงาน/สิ่งแวดล้อม
  (จาก AIO), ความปลอดภัย (จาก SEC-7), การใช้งานระบบ — แต่ละมิติมีการ์ด
  สรุป 3 ใบ ใช้ `ExecutiveResponsiveGrid` (ยกแพทเทิร์นจาก
  `FacilityResponsiveGrid` มา ไม่ import ข้ามโฟลเดอร์ ตามธรรมเนียมเดิม)
- **ทุกตัวเลขเป็นค่าสรุป/เฉลี่ยระดับโรงเรียน ไม่มีชื่อ/รหัสนักเรียนที่ไหน
  เลยตาม BR1** — ต้องคงกติกานี้ไว้เสมอเวลาแก้ไขหน้านี้ต่อไป

`executive_shared_widgets.dart` — `ExecutiveTheme` (สี indigo เข้มเป็นสีหลัก
ของ role นี้ ส่วนสีความหมายแดง/เขียว/ส้มใช้ค่าเดียวกับ FacilityTheme เพื่อ
ความคุ้นเคย), `ExecutiveResponsiveGrid`, `ExecutiveGlassCard`

**main.dart**: เพิ่ม route `/prototype/executive` → `ExecutiveDashboardPage`
— แก้แบบเดียวกับที่ทำให้ facility (คัดแยกเฉพาะส่วนของ executive ออกจากไฟล์
ที่มีงานค้างของ agy ฝั่งครูปนอยู่ ไม่แตะส่วนนั้นเลย)

`flutter analyze`: 482 issues เท่าเดิม (baseline เดิม ไม่มี error เพิ่ม)

## ⚠️ บทเรียนสำคัญเรื่อง main.dart (เจอตอนทำหน้า 1 — จำไว้เสมอ)

เดิมแก้ `main.dart` ด้วยเทคนิค `git hash-object` + `update-index` เพื่อ
คัดแยกเฉพาะส่วนของเรา ไม่ปนกับงานค้างของ agy ในไฟล์เดียวกัน — วิธีนี้
**เขียนเข้าไปแค่ใน git index (พื้นที่เตรียม commit) เท่านั้น ไม่เคยเขียนลง
ไฟล์จริงบนดิสก์เลย** ทำให้ commit ถูกต้องบน GitHub แต่แอปที่รันอยู่จริง
(`flutter run`) ยังใช้ไฟล์เก่าตลอดมา — route `/prototype/facility-storybook`
เวอร์ชันใหม่และ `/prototype/executive` ที่เพิ่มไปก่อนหน้านี้เลยไม่ทำงานจริง
จนผู้ใช้ลองเปิดแล้วเจอว่าไม่ตรง

**แก้แล้ว + วิธีทำที่ถูกต้องต่อไปนี้**: แก้ไฟล์จริงบนดิสก์ตรงๆ ด้วย Edit
tool ก่อนเสมอ (เฉพาะส่วนของเรา ไม่แตะบรรทัดของ agy) แล้วค่อยสร้างเวอร์ชัน
สะอาด (HEAD + ส่วนของเราเท่านั้น ไม่มีของ agy) แยกไว้ต่างหากเพื่อ stage
เข้า git index ด้วย `hash-object`/`update-index` สำหรับ commit — ทำ 2 ขั้น
นี้คู่กันเสมอ ห้ามข้ามขั้นแรก (แก้ไฟล์จริง) เด็ดขาด

## ✅ สร้างแล้ว (2026-08-15) — หน้า 2: ศูนย์แจ้งเตือน/เคสที่ต้องตัดสินใจ

`executive_escalation_inbox_page.dart` — รวม 2 จุดที่พบตอนตรวจวอลต์ (ทั้งคู่
เป็น Exception Flow ที่ดึง ผอ เข้ามาเป็นเส้นทางสำรอง ไม่ใช่ผู้รับผิดชอบหลัก):
1. **EMG-4 (Exception)** — เหตุฉุกเฉินที่ไม่มีใครรับทราบทันเวลา ยกระดับมา
   ผอ กดปุ่ม "รับทราบ" ได้
2. **SEC-5 (Exception)** — เหตุการณ์กล้อง AI ที่ครูไม่แน่ใจ ส่งมาให้ ผอ
   ช่วยตัดสินใจ (ยืนยันเป็นเหตุจริง / False Positive) — **ตั้งใจไม่ใส่ภาพ/
   พรีวิวกล้อง** เก็บไว้แค่ Event Metadata เหมือน STK-10 ของผู้ดูแลอาคาร
   เพราะเป็นเรื่องละเอียดอ่อนด้าน PDPA ที่ยังไม่ได้ถามผู้ใช้ (ดูรายละเอียด
   ⚠️ ในคอมเมนต์หัวไฟล์)

เชื่อมกับหน้า 1 แล้ว: กระดิ่งแจ้งเตือนบน hero header ของ dashboard (มี badge
เลข mock) กดแล้วพาไปหน้านี้ — หน้านี้มีปุ่มย้อนกลับเมื่อถูก push มา
(`Navigator.canPop`) — เข้าตรงได้ทาง `/prototype/executive-inbox` ด้วย

## ✅ สร้างแล้ว (2026-08-16) — Shell + Exception Flow demo + เก็บงานให้ครบ

ผู้ใช้ขอ "ทำหน้าผอมาให้เสร็จ" — ปิดรายการค้างที่ทำได้เองครบ:

- **`executive_home_page.dart` (ใหม่)** — shell รวม 2 หน้าเป็นแท็บเดียวกัน
  (top bar + แท็บสลับ "ภาพรวม"/"ศูนย์แจ้งเตือน") แทนการเข้าถึงแบบ URL ตรงๆ/
  Navigator.push เดิม — ยังไม่ใช่ sidebar เต็มรูปแบบเหมือนฝั่งผู้ดูแลอาคาร
  เพราะมีแค่ 2 ปลายทาง (ถ้ามีหน้าเพิ่มค่อยพิจารณาเปลี่ยนทีหลัง)
- **`executive_dashboard_page.dart`** — เปลี่ยนจากหน้า standalone (มี
  Scaffold เอง) เป็น `ExecutiveDashboardContent` (content-only รับ
  `onOpenInbox` callback จาก shell) + เพิ่ม demo-state switcher (สวิตช์
  เล็กๆ ทดสอบ Exception Flow ของ LA-9 — สลับดูว่ามิติ "พลังงาน & สิ่งแวดล้อม"
  แสดงข้อความ "ยังไม่มีข้อมูล" แทนการ์ดตัวเลขได้ถูกต้อง)
- **`executive_escalation_inbox_page.dart`** — เปลี่ยนจากหน้า standalone
  เป็น `ExecutiveEscalationInboxContent` (content-only ตัด gradient header +
  ปุ่มย้อนกลับเดิมออก เพราะ shell มี top bar ของตัวเองแล้ว)
- **main.dart**: ทั้ง `/prototype/executive` และ `/prototype/executive-inbox`
  ชี้ไป `ExecutiveHomePage` (ต่างกันแค่ `initialIndex`) — แก้ไฟล์จริงบนดิสก์
  ตรงๆ ก่อนเสมอ ตามบทเรียนที่บันทึกไว้ด้านบน

`flutter analyze`: 482 issues เท่าเดิม (baseline เดิม ไม่มี error เพิ่ม)

## ✅ [ยืนยันแล้ว 2026-08-16] เพิ่มพรีวิวภาพกล้องในเคส SEC-5

ผู้ใช้ยืนยันให้ ผอ เห็นภาพจริงจากกล้องประกอบการตัดสินใจได้ (สอดคล้อง SEC-9
ที่เปิดช่องให้ "School Admin/ผู้บริหารกรณีจำเป็น" เข้าถึงภาพจริงอยู่แล้ว) —
เพิ่ม `_buildCameraSnapshot()` ในการ์ดแต่ละเคสของแท็บ "รอความเห็นจากกล้อง
AI" ตอนนี้เป็น **placeholder** (กรอบมืด + ป้าย "LIVE SNAPSHOT (mock)" +
ชื่อกล้อง/ตำแหน่ง) เพราะยังไม่มีระบบกล้อง/Storage จริงให้ดึงภาพมา

**จุดที่ต้องทำตอนต่อ Supabase จริง (บันทึกไว้ในคอมเมนต์หัวไฟล์แล้ว)**: ต้อง
ผ่าน SEC-9 policy check ก่อนคืนภาพเสมอ ไม่ใช่แค่เช็ค role เฉยๆ เพราะ SEC-9
เขียนไว้ว่า "กรณีจำเป็น" ไม่ใช่สิทธิ์อัตโนมัติของทุก ผอ ทุกครั้ง

`flutter analyze`: 482 issues เท่าเดิม — ตอนนี้**ไม่มีจุดค้างที่ต้องถาม
ผู้ใช้เพิ่มแล้ว** ฝั่ง ผอ เสร็จตามสโคปที่วางไว้ (LA-9 + escalation inbox)
