# Executive (ผู้บริหารสถานศึกษา / ผอ) Redesign Prototype

สถานะ: PROTOTYPE SANDBOX — mock data ทั้งหมด ยังไม่ผูก Supabase จริง
(บันทึกไว้ 2026-08-15 โดย Claude Code)

## ที่มา — ตรวจสอบวอลต์กลางก่อนเริ่มสร้าง

ต่างจากฝั่งผู้ดูแลอาคาร (`facility_redesign_prototype/`) ที่มี 7 ยูสเคสเฉพาะ
ทาง (STK-6..12) — ตรวจวอลต์กลาง (`AIoT-School-Lab-Vault`) แล้วพบว่า
**ผู้บริหารสถานศึกษา (ผอ) มียูสเคสเฉพาะทางแค่ตัวเดียว**:

- **LA-9** "ดูรายงานภาพรวมของสถานศึกษา" — Goal: เห็นภาพรวมทั้งโรงเรียน
  (การเรียนรู้ พลังงาน สิ่งแวดล้อม ความปลอดภัย การใช้ระบบ) ในหน้าเดียว
  กรองตามช่วงเวลา/ระดับชั้นได้ — **BR1 (สำคัญ): เห็นภาพรวมระดับโรงเรียน
  เท่านั้น ห้ามเห็นข้อมูลรายคนเชิงลึกที่กระทบความเป็นส่วนตัว**

สิทธิ์อื่นที่ ผอ มีเป็นสิทธิ์ที่ยืมมาจากยูสเคสของ role อื่น ไม่ใช่หน้าจอ
เฉพาะของตัวเอง:
- **LA-11** Export PDF/Excel (Primary Actor: ครู/ผู้บริหาร)
- **SEC-7** รายงานความปลอดภัยทั้งโรงเรียน (Primary Actor: ผู้บริหาร/School
  Admin — เป็นต้นทางที่ STK-10 ของผู้ดูแลอาคารเป็นแค่ตัวกรองย่อยระดับอาคาร)
- **EMG-4 (Exception Flow)** — ผอ เป็นผู้รับแจ้งเตือนฉุกเฉินตั้งแต่แรกร่วม
  กับครูประจำพื้นที่ และเป็น**เส้นทางยกระดับ**ถ้าไม่มีใครรับทราบทันเวลา
  (**ไม่ใช่ผู้ยืนยันปิดหลักตาม EMG-3 ซึ่งเป็นหน้าที่ของครูก่อน** — ผอ เป็น
  แค่ทางสำรอง)
- **SEC-5 (Exception Flow)** — ถ้าครูไม่แน่ใจว่า Alert จากกล้อง AI เป็นเหตุ
  จริงหรือ false positive จะถูกส่งต่อให้ School Admin/ผอ ช่วยตัดสินใจ

**ยังไม่ได้ทำ (เก็บไว้เป็นหน้า 2 ถ้าต้องการ)**: หน้า "ศูนย์แจ้งเตือน/เคสที่
ต้องตัดสินใจ" ครอบคลุม 2 จุดข้างบน (EMG-4 escalation + SEC-5 escalation) —
ยังไม่เริ่มสร้าง

## ✅ สร้างแล้ว (2026-08-15) — หน้า 1: Dashboard ภาพรวมสถานศึกษา (LA-9)

`executive_dashboard_page.dart` — หน้า standalone (มี Scaffold ของตัวเอง
ต่างจากฝั่งผู้ดูแลอาคารที่เป็น content-only ต่อกับ shell เพราะยังไม่มี
shell/nav ของ role นี้) ประกอบด้วย:
- Hero header ไล่เฉดสี indigo เข้ม (ธีมแยกจาก FacilityTheme ตั้งใจ ให้แต่ละ
  role มีธีมของตัวเอง) + ปุ่ม Export (LA-11) มุมขวา
- ตัวกรองช่วงเวลา/ระดับชั้น (ตาม LA-9 Main Flow ข้อ 3)
- 4 มิติ ตาม Main Flow ข้อ 2: ผลการเรียน (จาก LA-3), พลังงาน/สิ่งแวดล้อม
  (จาก AIO), ความปลอดภัย (จาก SEC-7), การใช้งานระบบ — แต่ละมิติมีการ์ด
  สรุป 3 ใบ ใช้ `ExecutiveResponsiveGrid` (ยกแพทเทิร์นจาก
  `FacilityResponsiveGrid` มา ไม่ import ข้ามโฟลเดอร์ ตามธรรมเนียมเดิม)
- **ทุกตัวเลขเป็นค่าสรุป/เฉลี่ยระดับโรงเรียน ไม่มีชื่อ/รหัสนักเรียนที่ไหน
  เลยตาม BR1** — ต้องคงกติกานี้ไว้เสมอเวลาแก้ไขหน้านี้ต่อไป

`executive_shared_widgets.dart` — `ExecutiveTheme` (สี indigo เข้มเป็นสีหลัก
ของ role นี้ ส่วนสีความหมายแดง/เขียว/ส้มใช้ค่าเดียวกับ FacilityTheme เพื่อ
ความคุ้นเคย), `ExecutiveResponsiveGrid`, `ExecutiveGlassCard`

**main.dart**: เพิ่ม route `/prototype/executive` → `ExecutiveDashboardPage`
— แก้แบบเดียวกับที่ทำให้ facility (คัดแยกเฉพาะส่วนของ executive ออกจากไฟล์
ที่มีงานค้างของ agy ฝั่งครูปนอยู่ ไม่แตะส่วนนั้นเลย)

`flutter analyze`: 482 issues เท่าเดิม (baseline เดิม ไม่มี error เพิ่ม)

## ⚠️ บทเรียนสำคัญเรื่อง main.dart (เจอตอนทำหน้า 1 — จำไว้เสมอ)

เดิมแก้ `main.dart` ด้วยเทคนิค `git hash-object` + `update-index` เพื่อ
คัดแยกเฉพาะส่วนของเรา ไม่ปนกับงานค้างของ agy ในไฟล์เดียวกัน — วิธีนี้
**เขียนเข้าไปแค่ใน git index (พื้นที่เตรียม commit) เท่านั้น ไม่เคยเขียนลง
ไฟล์จริงบนดิสก์เลย** ทำให้ commit ถูกต้องบน GitHub แต่แอปที่รันอยู่จริง
(`flutter run`) ยังใช้ไฟล์เก่าตลอดมา — route `/prototype/facility-storybook`
เวอร์ชันใหม่และ `/prototype/executive` ที่เพิ่มไปก่อนหน้านี้เลยไม่ทำงานจริง
จนผู้ใช้ลองเปิดแล้วเจอว่าไม่ตรง

**แก้แล้ว + วิธีทำที่ถูกต้องต่อไปนี้**: แก้ไฟล์จริงบนดิสก์ตรงๆ ด้วย Edit
tool ก่อนเสมอ (เฉพาะส่วนของเรา ไม่แตะบรรทัดของ agy) แล้วค่อยสร้างเวอร์ชัน
สะอาด (HEAD + ส่วนของเราเท่านั้น ไม่มีของ agy) แยกไว้ต่างหากเพื่อ stage
เข้า git index ด้วย `hash-object`/`update-index` สำหรับ commit — ทำ 2 ขั้น
นี้คู่กันเสมอ ห้ามข้ามขั้นแรก (แก้ไฟล์จริง) เด็ดขาด

## ✅ สร้างแล้ว (2026-08-15) — หน้า 2: ศูนย์แจ้งเตือน/เคสที่ต้องตัดสินใจ

`executive_escalation_inbox_page.dart` — รวม 2 จุดที่พบตอนตรวจวอลต์ (ทั้งคู่
เป็น Exception Flow ที่ดึง ผอ เข้ามาเป็นเส้นทางสำรอง ไม่ใช่ผู้รับผิดชอบหลัก):
1. **EMG-4 (Exception)** — เหตุฉุกเฉินที่ไม่มีใครรับทราบทันเวลา ยกระดับมา
   ผอ กดปุ่ม "รับทราบ" ได้
2. **SEC-5 (Exception)** — เหตุการณ์กล้อง AI ที่ครูไม่แน่ใจ ส่งมาให้ ผอ
   ช่วยตัดสินใจ (ยืนยันเป็นเหตุจริง / False Positive) — **ตั้งใจไม่ใส่ภาพ/
   พรีวิวกล้อง** เก็บไว้แค่ Event Metadata เหมือน STK-10 ของผู้ดูแลอาคาร
   เพราะเป็นเรื่องละเอียดอ่อนด้าน PDPA ที่ยังไม่ได้ถามผู้ใช้ (ดูรายละเอียด
   ⚠️ ในคอมเมนต์หัวไฟล์)

เชื่อมกับหน้า 1 แล้ว: กระดิ่งแจ้งเตือนบน hero header ของ dashboard (มี badge
เลข mock) กดแล้วพาไปหน้านี้ — หน้านี้มีปุ่มย้อนกลับเมื่อถูก push มา
(`Navigator.canPop`) — เข้าตรงได้ทาง `/prototype/executive-inbox` ด้วย

## ✅ สร้างแล้ว (2026-08-16) — Shell + Exception Flow demo + เก็บงานให้ครบ

ผู้ใช้ขอ "ทำหน้าผอมาให้เสร็จ" — ปิดรายการค้างที่ทำได้เองครบ:

- **`executive_home_page.dart` (ใหม่)** — shell รวม 2 หน้าเป็นแท็บเดียวกัน
  (top bar + แท็บสลับ "ภาพรวม"/"ศูนย์แจ้งเตือน") แทนการเข้าถึงแบบ URL ตรงๆ/
  Navigator.push เดิม — ยังไม่ใช่ sidebar เต็มรูปแบบเหมือนฝั่งผู้ดูแลอาคาร
  เพราะมีแค่ 2 ปลายทาง (ถ้ามีหน้าเพิ่มค่อยพิจารณาเปลี่ยนทีหลัง)
- **`executive_dashboard_page.dart`** — เปลี่ยนจากหน้า standalone (มี
  Scaffold เอง) เป็น `ExecutiveDashboardContent` (content-only รับ
  `onOpenInbox` callback จาก shell) + เพิ่ม demo-state switcher (สวิตช์
  เล็กๆ ทดสอบ Exception Flow ของ LA-9 — สลับดูว่ามิติ "พลังงาน & สิ่งแวดล้อม"
  แสดงข้อความ "ยังไม่มีข้อมูล" แทนการ์ดตัวเลขได้ถูกต้อง)
- **`executive_escalation_inbox_page.dart`** — เปลี่ยนจากหน้า standalone
  เป็น `ExecutiveEscalationInboxContent` (content-only ตัด gradient header +
  ปุ่มย้อนกลับเดิมออก เพราะ shell มี top bar ของตัวเองแล้ว)
- **main.dart**: ทั้ง `/prototype/executive` และ `/prototype/executive-inbox`
  ชี้ไป `ExecutiveHomePage` (ต่างกันแค่ `initialIndex`) — แก้ไฟล์จริงบนดิสก์
  ตรงๆ ก่อนเสมอ ตามบทเรียนที่บันทึกไว้ด้านบน

`flutter analyze`: 482 issues เท่าเดิม (baseline เดิม ไม่มี error เพิ่ม)

## ✅ [ยืนยันแล้ว 2026-08-16] เพิ่มพรีวิวภาพกล้องในเคส SEC-5

ผู้ใช้ยืนยันให้ ผอ เห็นภาพจริงจากกล้องประกอบการตัดสินใจได้ (สอดคล้อง SEC-9
ที่เปิดช่องให้ "School Admin/ผู้บริหารกรณีจำเป็น" เข้าถึงภาพจริงอยู่แล้ว) —
เพิ่ม `_buildCameraSnapshot()` ในการ์ดแต่ละเคสของแท็บ "รอความเห็นจากกล้อง
AI" ตอนนี้เป็น **placeholder** (กรอบมืด + ป้าย "LIVE SNAPSHOT (mock)" +
ชื่อกล้อง/ตำแหน่ง) เพราะยังไม่มีระบบกล้อง/Storage จริงให้ดึงภาพมา

**จุดที่ต้องทำตอนต่อ Supabase จริง (บันทึกไว้ในคอมเมนต์หัวไฟล์แล้ว)**: ต้อง
ผ่าน SEC-9 policy check ก่อนคืนภาพเสมอ ไม่ใช่แค่เช็ค role เฉยๆ เพราะ SEC-9
เขียนไว้ว่า "กรณีจำเป็น" ไม่ใช่สิทธิ์อัตโนมัติของทุก ผอ ทุกครั้ง

`flutter analyze`: 482 issues เท่าเดิม — ตอนนี้**ไม่มีจุดค้างที่ต้องถาม
ผู้ใช้เพิ่มแล้ว** ฝั่ง ผอ เสร็จตามสโคปที่วางไว้ (LA-9 + escalation inbox)

## ✅ [แก้ไข 2026-08-16] แก้ 2 จุด Major จากการตรวจสอบเทียบ UC เต็มรูปแบบ

ตรวจสอบทั้งโฟลเดอร์เทียบวอลต์แบบเดียวกับที่ทำครู/นักเรียน พบ 5 จุด แก้ 2 จุด
Major ก่อน (อีก 3 จุด Minor ยังไม่แก้ — ดูรายการด้านล่าง):

1. **STK-12 BR6/Exception 2 ตกหล่นตอน scope ครั้งแรก** — NOTES.md เดิม
   (ด้านบน) ไม่เคยพูดถึง STK-12 เลย ทั้งที่กติการะบุว่า Warning Light ที่
   ผู้ดูแลอาคารเปิดแบบ proactive (§3b) ต้องรอครู**หรือ ผอ**ยืนยันก่อนปิด
   เท่านั้น (ผู้ดูแลอาคารเปิดเองปิดเองไม่ได้) — เพิ่มแท็บที่ 3
   "ไฟเตือนรอยืนยันปิด" ใน `executive_escalation_inbox_page.dart` มีปุ่ม
   "ยืนยันปิดไฟเตือน" แยกจาก "รับทราบ" เดิม + จำลอง Exception 2 (เกิน 15
   นาทีไม่มีใครยืนยัน → แบนเนอร์ยกระดับไปยัง School Admin)
2. **ตัวกรองช่วงเวลา/ระดับชั้นใน Dashboard ไม่มีผลจริง** — เลือก dropdown
   ได้แต่การ์ดสถิติเป็นค่าคงที่ ไม่ตอบสนอง ทั้งที่ LA-9 Main Flow ข้อ 3
   ระบุชัดว่าต้องกรองได้ — เพิ่ม getter `_learningByGrade`,
   `_activeUsersByGrade`, `_energyByPeriod`, `_safetyEventsByPeriod`,
   `_periodTrendSuffix` ใน `executive_dashboard_page.dart` ให้ทั้ง 4 มิติ
   เปลี่ยนค่าจริงตามตัวกรองที่เลือก (มิติผลการเรียน+การใช้งานระบบ ตอบสนอง
   ทั้งช่วงเวลา/ระดับชั้น ส่วนพลังงาน+ความปลอดภัยตอบสนองตามช่วงเวลา เพราะ
   เป็นข้อมูลระดับอาคาร ไม่แยกตามระดับชั้นตามธรรมชาติของข้อมูล)

**3 จุด Minor ที่ยังไม่แก้ (ค้างไว้เป็นรายการทำต่อ)**:
- ภาพกล้องใน SEC-5 escalation ไม่มีช่อง "เหตุผลที่ต้องดู"/ข้อความแจ้งว่าถูก
  audit ตาม SEC-9 BR2 (โค้ดมี TODO เตือนไว้แล้วในคอมเมนต์หัวไฟล์)
- ปุ่ม Export ไม่มีให้เลือกฟอร์แมต PDF/Excel ตาม LA-11 Main Flow ข้อ 2
- การตัดสินใจ SEC-5 ไม่บันทึก/แสดงว่าใครเป็นคนตัดสินใจ (`decidedBy`/
  `decidedAt`) ตาม SEC-5 Postcondition

`flutter analyze`: 482 issues เท่าเดิม (baseline เดิม ไม่มี error เพิ่ม)

## ✅ [แก้ไข 2026-08-16] แก้ 3 จุด Minor ที่เหลือ — ครบทั้ง 5 จุดจากการตรวจสอบ

1. **SEC-9 BR2 audit UX** — เพิ่มข้อความ "การเข้าถึงภาพนี้ถูกบันทึกลง Audit
   Log ตาม SEC-9 BR2" ใต้ภาพกล้องเสมอ (ไม่มีเงื่อนไข) + บังคับกรอก
   "เหตุผลที่ต้องดูภาพนี้" ในช่อง TextField ก่อนปุ่ม "ยืนยันเป็นเหตุจริง"/
   "False Positive" จะกดได้ (`_accessReasonControllers` คุมสถานะ enable/
   disable) — สอดคล้อง SEC-9 "กรณีจำเป็น" ไม่ใช่สิทธิ์อัตโนมัติ
2. **LA-11 Export format picker** — เปลี่ยนปุ่ม Export จากยิง snackbar
   ทันทีเป็นเปิด bottom sheet ให้เลือก PDF หรือ Excel ก่อนเสมอ
   (`_showExportFormatPicker`) ตรงตาม Main Flow ข้อ 2 ที่ระบุว่าต้องเลือก
   รูปแบบไฟล์
3. **SEC-5 decidedBy/decidedAt** — เพิ่ม field ในโมเดล mock + แสดงผล
   "โดย [ชื่อ ผอ] · [เวลา]" ใต้ป้ายผลตัดสินใจเมื่อกดยืนยัน/ปฏิเสธแล้ว
   (mock ชื่อ ผอ คงที่ `_currentExecutiveName` เพราะโฟลเดอร์นี้ยังไม่ผูก
   auth จริง — ตอนต่อ Supabase ต้องดึงชื่อจาก session จริงแทน)

`flutter analyze`: 482 issues เท่าเดิม (baseline เดิม ไม่มี error เพิ่ม) —
ครบทั้ง 5 จุดจากรายงานตรวจสอบเทียบ UC แล้ว

## 💎 [เชื่อมข้อมูลจริง 2026-08-16] Dashboard 2 ใน 4 มิติเชื่อมกับ Supabase จริงแล้ว

หลังสลับ `RoleRouter` ให้ ผอ ที่ login จริงเจอหน้านี้แทนหน้าเก่า พบว่าทุก
ตัวเลขเป็น mock — ตรวจสอบ RPC ที่มีอยู่ทั้งหมดพบว่า **role `executive`
ไม่ได้รับอนุญาตให้เรียก RPC สรุปข้อมูลโรงเรียนตัวไหนเลยสักตัว** (ต่างจาก
ครูที่ backend มีอยู่แล้วแค่รอต่อสาย Flutter) จึงต้องเพิ่ม backend ใหม่ก่อน:

**Migration ใหม่**: `supabase/migrations/20260816000000_executive_school_stats.sql`
1. `count_school_users_by_role(p_token)` — RPC ใหม่ คืน **แค่ {role, count}**
   เท่านั้น ไม่มีชื่อ/อีเมล/รหัสรายคนเลย — ตั้งใจไม่เปิด `list_school_users`
   ตรงๆ ให้ ผอ (คืนรายชื่อ/อีเมลทุกคนในโรงเรียน) เพราะจะขัด **BR1 ที่ล็อกไว้
   บนสุดของไฟล์นี้ทันที** — ผู้ใช้เลือกทางนี้เองตอนถามว่าจะแก้ยังไง (มี
   ตัวเลือก "เปิด list_school_users ตรงๆ" ให้เลือกด้วยแต่เลือกทางที่
   ปลอดภัยกว่า)
2. `list_school_devices` — เพิ่ม `'executive'` เข้า role ที่อนุญาต (เดิมมีแค่
   teacher/school_admin) ตัวนี้ปลอดภัยเพราะคืนแค่ข้อมูลอุปกรณ์ ไม่มีข้อมูล
   บุคคล
3. pgTAP ใหม่ `supabase/tests/database/15_executive_school_stats.test.sql`
   (9 assertions: executive เรียกได้/นับถูกต้อง/isolate ข้ามโรงเรียน/
   student ยังถูกบล็อกทั้งสอง RPC) — **ยังไม่เคยรันจริง เพราะเครื่องนี้ไม่มี
   Docker** เหมือนเดิม ต้องให้ agy (มี Docker) รัน `npx supabase db reset
   && npx supabase test db` ยืนยันก่อน

**Flutter**: เพิ่ม `UserAdminService.countUsersByRole()` ใน
`packages/shared_core/lib/services/user_admin_service.dart` (คู่กับ
`LessonService.listSchoolDevices()` ที่มีอยู่แล้ว) แล้วเชื่อมเข้า
`executive_dashboard_page.dart`:
- **มิติ "การใช้งานระบบ"** — เปลี่ยนจาก "ผู้ใช้งาน active วันนี้"/
  "Uptime"/"อัตราล็อกอิน" (ตัวเลขปลอมที่คำนวณไม่ได้จริง) เป็นผู้ใช้ทั้งหมด
  จริง (แยกครู/นักเรียน/ผู้ปกครอง), จำนวนอุปกรณ์จริง, % อุปกรณ์ออนไลน์จริง
- **มิติ "พลังงาน & สิ่งแวดล้อม"** — เปลี่ยนจาก kWh ปลอมเป็นจำนวนเซนเซอร์
  สิ่งแวดล้อมจริง (`pm25_sensor`/`air_quality_sensor`/`light_sensor`/
  `energy_meter`), % ออนไลน์จริง, จำนวนพื้นที่ที่มีเซนเซอร์จริง (นับจาก
  `location` จริงของอุปกรณ์) — Exception Flow เดิม (แสดง "ยังไม่มีข้อมูล"
  ถ้าไม่มีเซนเซอร์เลย) ยังทำงานถูกต้อง ทดสอบได้ทั้งจาก demo switch เดิม
  และจากกรณีจริงที่ยังไม่มีเซนเซอร์เลย
- **ตัวกรองช่วงเวลา/ระดับชั้นไม่มีผลกับ 2 มิตินี้อีกต่อไป** (บอกไว้ตรงๆ ใน
  subtitle "ข้อมูล ณ ปัจจุบัน ไม่แยกตามช่วงเวลา/ระดับชั้น") เพราะไม่มี RPC
  สรุปย้อนหลังให้ ต่างจากตอนที่เป็น mock ที่ทำให้ตัวเลขเปลี่ยนตามได้ทุกครั้ง
  — เลือกความถูกต้อง (ไม่โกหกว่ากรองได้) มากกว่าความสวยงามของ demo

**ยังเป็น mock อยู่ (ตรวจแล้วว่าไม่มี backend รองรับเลย ไม่ใช่แค่ยังไม่ได้
ต่อสาย)**:
- มิติ "ผลการเรียน" — ไม่มี RPC สรุปคะแนนทั้งโรงเรียนเลย
- มิติ "ความปลอดภัย" — ไม่มีระบบ incident/EMG/SEC ในฐานข้อมูลเลยทั้งระบบ
  (เจอจุดเดียวกันตอนตรวจ facility/STK-12 ก่อนหน้านี้)
- `executive_escalation_inbox_page.dart` ทั้งหน้า (EMG-4/SEC-5) — ไม่มี
  backend รองรับเลยเช่นกัน ยังไม่ได้แตะ

## 💎 [เชื่อมข้อมูลจริง 2026-08-17] มิติ "ความปลอดภัย" ใน Executive Dashboard เชื่อมกับ Supabase จริงแล้ว

- เชื่อมต่อ `IncidentService.getIncidentSummary()` (RPC `get_incident_summary`) เข้ากับ มิติ "ความปลอดภัย" ใน `executive_dashboard_page.dart`
- คำนวณสรุปรายงานเหตุการณ์ทั้งหมด, จำนวน SOS, เวลาตอบสนองเฉลี่ยของครูจริง
- เป็นข้อมูลสรุปภาพรวมระดับโรงเรียน ไม่มี PII หรือข้อมูลที่ระบุตัวตนนักเรียนรายคน หลุดรอดออกมา สอดคล้องตามกฎ BR1 และ PDPA 100%
- `flutter analyze` 478 issues (0 error) ผ่านสะอาดเรียบร้อย

## ⚡💧 [เชื่อมข้อมูลจริง 2026-08-17] สรุปค่าไฟฟ้าและค่าน้ำโดยประมาณ (AIO-5/AIO-6 + มิเตอร์น้ำเพิ่มใหม่)

- **Backend Migrations**:
  - `20260819000000_water_utility_schema.sql` — เพิ่ม `water_meter` เข้า enum `device_type`, `water_m3` เข้า enum `metric_type`, และคอลัมน์ `water_rate_thb` ใน `school_settings`
  - `20260819010000_utility_costs.sql` — สร้าง RPC 4 ตัว: `get_school_utility_rates`, `set_school_utility_rates`, `get_energy_usage_summary`, `get_water_usage_summary`
- **Business Rules & PDPA Compliance**:
  - กำหนด disclaimer สตรีมคำเตือน AIO-6 BR1 ชัดเจนทุก response: *"ค่าบริการประมาณการเพื่อการบริหารจัดการภายใน ไม่ใช่ใบแจ้งหนี้จริงจากผู้ให้บริการ"*
  - กรณีโรงเรียนยังไม่มีมิเตอร์น้ำติดตั้ง (`device_count = 0`) ระบบแสดงผล empty state "ยังไม่มีมิเตอร์น้ำติดตั้ง" โดยไม่แสดง 0 บาท เพื่อป้องกันการเข้าใจผิด
- **pgTAP Test Suite**: เพิ่ม `20_utility_costs.test.sql` ทดสอบการตั้งค่าอัตรา, การกักกันสิทธิ์ (เฉพาะ school_admin), และการคำนวณสรุปยอด (PASS 100%)
- **Flutter**: สร้าง `UtilityService` และ `utility_model.dart` ใน `shared_core` แล้วเชื่อมต่อเข้า มิติ "พลังงาน & สิ่งแวดล้อม" ใน `executive_dashboard_page.dart` เรียบร้อย

## 🛠️ [Fix 2026-08-18] แก้ไขบั๊กช่องโหว่สิทธิ์ RPC และการแสดงผล UI ค่าไฟ/ค่าน้ำ

- **บั๊กที่ 1 (ช่องโหว่สิทธิ์ RPC)**:
  - เพิ่ม role guard ใน RPCs `get_school_utility_rates`, `get_energy_usage_summary`, `get_water_usage_summary` ป้องกันบทบาท `parent` เข้าถึงข้อมูลสถิติพลังงานและอัตราค่าน้ำ/ค่าไฟตาม permission matrix AIO-1..6 (ตอบกลับเป็น `forbidden`)
  - บล็อกสิทธิ์ `super_admin` สำหรับ RPC สรุปพลังงาน AIO ตามทางเลือก A1 จากผู้ใช้ (ตรงตามตาราง Permission Matrix)
  - เพิ่ม pgTAP assertions ใน `20_utility_costs.test.sql` ทดสอบการปฏิเสธสิทธิ์ `parent` และ `super_admin` (`PASS 100%`)
- **บั๊กที่ 2 (การแสดงผล UI บน Executive Dashboard)**:
  - เพิ่มการ์ดจริงแสดงตัวเลขประมาณการค่าไฟฟ้า (`฿estimatedCostThb` + kWh/อัตรา) และประมาณการค่าน้ำประปา (`฿estimatedCostThb` + m³/อัตรา) ใน `executive_dashboard_page.dart`
  - รองรับกรณีไม่มีมิเตอร์น้ำติดตั้ง (`deviceCount == 0`): แสดงข้อความ **"ยังไม่มีมิเตอร์น้ำติดตั้ง"** บนการ์ดอย่างชัดเจน (ไม่แสดง 0 บาท)
  - แสดงป้ายเตือนอัตราเริ่มต้น (`isRateDefault == true`) และแสดงกล่องคำเตือน AIO-6 BR1 Disclaimer *"ค่าบริการประมาณการเพื่อการบริหารจัดการภายใน ไม่ใช่ใบแจ้งหนี้จริงจากผู้ให้บริการ"* ใต้ตารางการ์ดอย่างเด่นชัด



