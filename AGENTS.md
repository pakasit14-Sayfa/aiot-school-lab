# กติกาสำหรับ AI ทุกตัวที่เข้ามาทำงานในโปรเจกต์นี้

โปรเจกต์นี้มี AI หลายตัวและหลายบัญชีสลับกันทำงาน ไฟล์นี้คือสิ่งที่ต้องรู้ก่อนแตะอะไร
(Claude อ่าน `CLAUDE.md` อัตโนมัติ — ไฟล์นี้สำหรับ Cursor / Copilot / Codex /
ChatGPT / Gemini และตัวอื่น ๆ เนื้อหากติกาเหมือนกัน)

## เริ่มด้วยคำสั่งเดียว

```bash
./scripts/state.sh
```

พ่นสถานะจริง ณ ตอนนี้ ~40 บรรทัด: งานไหน push แล้ว/ยัง · เลนอื่นทำอะไรอยู่ ·
build เขียวไหม · หน้าไหนต่อ backend แล้ว · RPC ตัวไหนเป็นกับดัก · อะไรค้างอยู่

**ห้ามอ่านสถานะจากเอกสารแล้วเชื่อเลย** — ดูหัวข้อ "ทำไมถึงไม่ให้เชื่อเอกสาร" ข้างล่าง

---

## 3 ข้อที่ผิดแล้วพังเงียบ ไม่มี error ให้เห็น

### 1. ไม่ใช่ Supabase Auth

ไม่มี `auth.users` ไม่มี `auth.uid()` ระบบใช้ตาราง `sessions` ของตัวเอง +
`session_token` ที่เป็น string ทึบ ตรวจผ่าน `get_session_actor(p_token)` ในทุก RPC

**ทุก RPC ที่แอปเรียกต้องรับ `p_token` เป็นอาร์กิวเมนต์แรก**

⚠️ ใน repo นี้มีสองแอป: `my_first_app` (แอปหลัก ใช้ session token) และ
`aiot_dev_dashboard` (ใช้ Supabase Auth จริง) **RPC ของสองแอปปนอยู่ในฐานข้อมูลเดียวกัน**
ตัวที่ไม่มี `p_token` และใช้ `is_super_admin()` / `has_role()` / `current_user_school_id()`
เป็นของแอปที่สอง — เรียกจาก `my_first_app` แล้ว **actor เป็น null เงียบ ๆ ไม่ error**
หาสาเหตุยากมาก

หลายตัวมีคู่แฝดชื่อ `*_for_school_admin` ที่ใช้ได้ — `./scripts/state.sh` §4 จับคู่ให้แล้ว
**ตรวจลายเซ็นก่อนเสมอ อย่าดูแค่ชื่อ**

### 2. RLS เป็น deny-all ทุกตาราง ไม่มี policy สักอัน

`supabase.from('table').select()` จะ **คืนค่าว่างเงียบ ๆ ไม่ error**
อ่าน/เขียนทุกอย่างต้องผ่าน RPC ที่เป็น `SECURITY DEFINER`
เพิ่มฟีเจอร์ = เพิ่ม RPC ไม่ใช่เปิดตาราง

### 3. เส้นทางข้อมูลมีทางเดียว

```
Page (บาง) → Controller (ถือ state) → Service (shared_core) → RPC → ตาราง
```

หนึ่ง service ต่อหนึ่งโดเมน ห้ามเรียก `supabase.rpc(...)` ตรงจากหน้า
**ก่อนเขียน service ใหม่ ให้เช็คก่อนว่ามีอยู่แล้วไหม** — 2026-09-06 มีการเขียน
`StaffInvitation` และเมธอด invitation ซ้ำทั้งที่มีอยู่แล้ว ทำให้ export ชนกันและ
ทั้ง repo คอมไพล์ไม่ผ่าน test พังยกชุด 51 ตัว

---

## เกณฑ์ว่าหน้าไหน "เสร็จ"

หน้าหนึ่งจะนับว่าเสร็จได้ต้องครบทุกข้อ:

- [ ] ข้อมูลที่แสดงมาจาก backend จริง หรือบอกว่า `ยังไม่มีข้อมูล` — **ห้ามมีค่า fallback**
- [ ] แยก **loading / data / empty / error** ออกจากกันได้ ผู้ใช้ต้องบอกความต่างได้
- [ ] mutation ต้อง **เขียน → อ่าน canonical กลับ → ตรวจว่าเจอจริง → ค่อยบอกสำเร็จ**
- [ ] ปุ่มที่ไม่มี backend = **disable + บอกเหตุผล** ไม่ใช่ปุ่มที่กดแล้วขึ้นข้อความขอโทษ
- [ ] ตัวเลือกใน filter/dropdown ต้องกรองได้จริง — สร้างจากข้อมูล ไม่ใช่รายการเขียนมือ
- [ ] ไม่มี error ดิบ (`$e`) ขึ้นจอ
- [ ] มี widget test ครอบทั้ง 4 สถานะ + pgTAP ถ้าแตะ backend
- [ ] รัน regression ทั้งชุด ต้องไม่เพิ่มจำนวน fail
- [ ] เปิดเบราว์เซอร์คลิกจริง

ตัวอย่างที่ทำถูกครบแล้ว ใช้เป็นแบบได้: `school_admin_cctv_page` ·
`school_learning_tracks_page` · `school_admin_device_schedule_page`
(ดู controller ของมันเป็นหลัก — โยน `backend_*_not_confirmed` เมื่อ backend ไม่ยืนยัน)

---

## กติกา commit — นี่คือหน่วยความจำร่วมของ AI ทุกตัว

เอกสารสรุปงานเน่าเสมอ commit ไม่เน่าเพราะผูกกับการเปลี่ยนแปลงจริง
`./scripts/state.sh --log` สร้างประวัติงานจาก git ให้อ่านได้ทันที **ถ้า commit เขียนดี**

- **1 ticket = 1 commit** ห้ามรวมหลายเรื่องใน commit เดียว
- เนื้อ commit ต้องตอบ 3 ข้อ: **แก้อะไร · ทำไมของเดิมผิด · เหลืออะไรที่ยังไม่ได้ทำ**
- ใส่ `Co-Authored-By:` บอกว่า AI ตัวไหนทำ
- **push ทุกครั้งที่จบ ticket** ไม่งั้นตัวอื่นมองไม่เห็นและจะทำงานทับ

### จองงานด้วย branch ไม่ต้องมีไฟล์ล็อก

แยกเลนตามโฟลเดอร์ที่ไม่ชนกัน — `pages/school_admin/` กับ
`executive_redesign_prototype/` `./scripts/state.sh` §1 โชว์ว่าเลนไหนเพิ่งขยับเมื่อไหร่
เห็นว่าเลนหนึ่งขยับเมื่อ 5 นาทีก่อนก็เลี่ยงไปทำอีกเลน

⚠️ **migration ต้องคุยกันก่อน** — สองตัวสร้าง migration พร้อมกันจะได้ timestamp ชนกัน
และ RPC ที่ใช้ร่วมหลายหน้าต้องให้เลนเดียวเขียน อีกเลนรอ

⚠️ **`git status`/`flutter test` ที่อ่านไว้เมื่อสักครู่ใช้ไม่ได้ทันทีที่เลนอื่นกำลังทำงานอยู่** —
2026-09-07 สองบัญชี (Claude app + Claude Code เทอมินอล) ทำงานในโฟลเดอร์เดียวกันพร้อมกัน
เลนหนึ่งแก้ `school_permissions_page.dart` (ปิดปุ่ม "เพิ่มสิทธิ์" ที่เป็นของปลอม) อีกเลนหนึ่ง
commit หน้า Executive เข้ามาระหว่างนั้น พอรัน regression รอบแรกจึงมี test fail 2 ตัวที่มา
จากงานของอีกเลนที่ยัง**ไม่ได้ commit ตอนที่อ่านค่าไว้** — นี่คือบั๊กชนิดเดียวกับ "เชื่อเอกสารแล้วพัง"
ใน §ทำไมถึงไม่ให้เชื่อเอกสาร แค่ย่นเวลาที่ context จะเน่าลงเหลือไม่กี่นาทีแทนที่จะเป็นวัน
**ถ้ารู้ว่ามีอีกเลนกำลังทำงานสด ๆ อยู่ (ไม่ใช่แค่ push ล่าสุดเมื่อไหร่) ต้องอ่าน `git status` /
รัน `flutter test` ใหม่ก่อนสรุปผลเสมอ** ห้ามอ้างอิงผลที่อ่านไว้ก่อนหน้าข้ามเทิร์นการสนทนา

---

## ทำไมถึงไม่ให้เชื่อเอกสาร

2026-09-06 เอกสารใน `docs/handoff/` ผิดพร้อมกัน 3 จุด ทั้งที่เขียนไว้อย่างมั่นใจ:

| เอกสารบอก | ความจริง |
|---|---|
| baseline test fail 16 ชุด | ของจริง 17 |
| `school_admin_energy_page` "ต่อครบแล้ว เหลือแค่งาน DoD" | มี fallback ปลอม 10 ตัวที่ลอกมาจากไฟล์ screenshot test · บล็อกรายอาคารปลอมทั้งก้อน · insights ที่อ้างว่า "ตรวจพบเครื่องปรับอากาศเปิดเกิน 8 ชม." ทั้งที่ระบบไม่มีการติดตามอุปกรณ์รายตัว |
| `school_alerts_page` "ผ่าน DoD แล้ว" | filter 4 ตัวเป็นรายการเขียนตายตัวที่กรองอะไรไม่ได้เลย รวมถึงชื่ออาคาร 5 ชื่อที่แต่งขึ้น |

และก่อนหน้านั้น CLAUDE.md บันทึกไว้เองว่าเคยมีเซสชันอ่านเอกสารแล้วเข้าใจสถานะผิดทั้งหมด

**กฎที่ตามมา:**
1. รัน `./scripts/state.sh` ก่อน อย่าเริ่มจากการอ่านเอกสาร
2. **`grep` พิสูจน์ได้แค่ "ไม่ต่อ backend แน่ ๆ" ไม่เคยพิสูจน์ว่า "ต่อครบแล้ว"**
   ต้องอ่านทั้งไฟล์ก่อนสรุปว่าหน้าไหนเสร็จ
3. ตรวจ role gate และลายเซ็น RPC กับ **ฐานข้อมูลที่รันอยู่** ไม่ใช่ไฟล์ migration —
   โปรเจกต์นี้มีประวัติ "ไฟล์ migration มีอยู่ แต่ไม่เคยถูกรันจริง" ซ้ำหลายรอบ
4. ถ้าเจอว่าเอกสารผิด **แก้เอกสารในเซสชันเดียวกัน** และเขียนไว้ใน commit ว่าแก้อะไร

---

## รันในเครื่อง

```bash
open -a Docker && npx supabase start        # ไม่มี supabase CLI แบบ global ใช้ npx เสมอ
cd apps/user_app && flutter run -d chrome --dart-define-from-file=../../env.json
```

บัญชีทดสอบ (รหัสเดียวกันหมด `Test1234!`): `schooladmin@` `teacher@` `student@`
`parent@` `admin@` `executive@` ต่อท้ายด้วย `aiot-school-lab.local`
โหมด dev จะโชว์ OTP บนหน้าจอให้เลย

**ตรวจ analyze ทั้ง 3 แพ็กเกจเสมอ** — `flutter analyze` ใน `apps/user_app`
ไม่ครอบ `packages/shared_ui` เคยปล่อยของที่คอมไพล์ไม่ผ่านหลุดไปแล้วเพราะเช็คแค่ตัวเดียว

---

## อ่านต่อ

| ไฟล์ | คืออะไร |
|---|---|
| `CLAUDE.md` | สถาปัตยกรรม + กติกาฉบับเต็ม |
| `docs/handoff/SCHOOL_ADMIN_BACKEND_SURVEY.md` | แผนรายหน้า School Admin — หน้าไหนต่อได้เลย หน้าไหนต้องเขียน RPC ใหม่ |
| `docs/handoff/EXECUTIVE_BACKEND_SURVEY.md` | เหมือนกันแต่ฝั่ง Executive |
| `docs/handoff/DATA_CONNECTION_METHODOLOGY.md` | วิธีตรวจว่าหน้าไหนมีข้อมูลปลอม |
| `docs/handoff/WORK_LOG.md` | ส่วนบนสุด = ตัดสินใจที่ค้างอยู่ + งานค้างตอนนี้ · ส่วนล่าง = ประวัติการตรวจที่ไม่ได้เกิด commit |
| `docs/handoff/DATABASE_SCHEMA.md` | สคีมาเต็ม — สร้างใหม่จาก DB อย่าแก้มือ |
