# บันทึกปัญหา (Pitfalls)

ที่เก็บ **กับดักที่กินเวลาไปแล้วอย่างน้อยหนึ่งรอบ** — อาการที่ดูเหมือนอย่างหนึ่ง
แต่สาเหตุอยู่อีกที่ เพื่อไม่ให้เซสชันถัดไป (คนหรือ AI) เสียเวลาซ้ำกับเรื่องเดิม

**ไฟล์นี้ไม่ใช่ WORK_LOG** — `WORK_LOG.md` บันทึกว่า *ทำอะไรไป* ส่วนไฟล์นี้บันทึกว่า
*อะไรหลอกเรา* และวิธีจับได้เร็วขึ้นในครั้งหน้า

## วิธีเพิ่มรายการ

เพิ่มต่อท้าย ใช้หัวข้อ `## <วันที่> — <อาการที่เห็น>` แล้วเขียนให้ครบ 5 บรรทัดนี้
ห้ามข้าม "วินิจฉัยผิดที่เคยทำ" เพราะนั่นคือส่วนที่ช่วยคนอ่านมากที่สุด:

- **อาการ** — สิ่งที่เห็นบนจอ/ในเทอร์มินัล ไม่ใช่สิ่งที่คิดว่าเป็น
- **วินิจฉัยผิดที่เคยทำ** — เดาผิดว่าอะไร และเสียเวลาไปกับอะไร
- **สาเหตุจริง** — พร้อมไฟล์:บรรทัด
- **วิธีแก้** — commit ที่แก้
- **วิธีกันซ้ำ** — กฎที่ใช้ได้ครั้งหน้า

---

## 2026-09-21 — ช่องกรอกมีกรอบสองชั้น กรอบในเป็นสีฟ้าตอนโฟกัส

- **อาการ** — วาด `Container` ขอบมนของตัวเองครอบ `TextField` แล้วมีกรอบมนอีกวง
  โผล่ข้างใน ตอนไม่โฟกัสเป็นเส้นเทาบาง ตอนโฟกัสเป็นเส้นฟ้าหนา 2
- **วินิจฉัยผิดที่เคยทำ** — เชื่อว่าเป็น focus ring ของ iOS ไปปิด
  *Connect Hardware Keyboard* ในซิมูเลเตอร์ (ผลข้างเคียง: วางข้อความด้วย ⌘V
  ไม่ได้ทั้งเครื่อง) แล้วเดาต่อว่าเป็น *Full Keyboard Access* ก็ไม่ใช่อีก
  เสียไปสองรอบเพราะยังไม่ได้เปิด `app_theme.dart` ดูเลย
- **สาเหตุจริง** — `apps/user_app/lib/theme/app_theme.dart:84-95` ตั้ง
  `inputDecorationTheme` ไว้ทั้งชุด (`filled: true`, `enabledBorder` เทา,
  `focusedBorder` ฟ้าหนา 2) ค่าเหล่านี้ถูกเติมให้ช่องกรอก**ทุกช่องในแอป**
  อัตโนมัติผ่าน `decoration.applyDefaults(theme)` และ `focusedBorder`/
  `enabledBorder` **ชนะ** `border: InputBorder.none` เสมอ เพราะ `border`
  เป็นเพียงค่าสำรองเมื่อสถานะนั้น ๆ เป็น null
- **วิธีแก้** — `9d71e5a` ปิดทุกสถานะในช่องกรอกของหน้านั้น
  (`enabledBorder` / `focusedBorder` / `disabledBorder` / `errorBorder` /
  `focusedErrorBorder` = `InputBorder.none`) และ `filled: false`
- **วิธีกันซ้ำ** — จะวาดกรอบเองรอบ `TextField` ให้ใช้
  `InputDecoration.collapsed(hintText: …)` ซึ่งปิด border/filled/padding
  ทั้งชุดในบรรทัดเดียว · และถ้าเห็นเส้นที่ "ไม่ได้สั่ง" ให้เปิดธีมของแอปก่อน
  เดาว่าเป็นของระบบปฏิบัติการ (ของระบบจะวาดคร่อมทั้ง widget ส่วนของธีมจะวาด
  ชิดขอบในกรอบที่เราวาด)

## 2026-09-21 — แถบความคืบหน้าหายทั้งแถบ ทั้งที่โค้ดถูกและไม่มี error

- **อาการ** — `Row` ของ `Expanded` + `ColoredBox` ที่ใช้ทำแถบสัดส่วน ไม่ปรากฏ
  บนจอเลย พื้นที่ว่างเปล่า ไม่มี overflow ไม่มี error
- **สาเหตุจริง** — `ColoredBox` ที่ไม่มีลูกไม่มีความสูงในตัวเอง และ `Row`
  จัด `crossAxisAlignment.center` เป็นค่าเริ่มต้น ลูกจึงได้ constraint ความสูง
  แบบหลวมแล้วยุบเหลือ 0
- **วิธีแก้** — `87d7b9c` ใส่ `crossAxisAlignment: CrossAxisAlignment.stretch`
- **วิธีกันซ้ำ** — widget ที่ไม่มีลูก (`ColoredBox` / `SizedBox.shrink` /
  `DecoratedBox`) ต้องมีคนกำหนดขนาดให้เสมอ — `stretch`, `SizedBox` ที่ระบุ
  ความสูง หรือใส่ลูกที่มีขนาด · และให้เรนเดอร์ภาพดูก่อนส่งขึ้นเครื่อง
  (`matchesGoldenFile` + `--update-goldens`) ~10 วินาที เจอทันที

## 2026-09-21 — ข้อความบนปุ่มหลุดฟอนต์ของแอป

- **อาการ** — ป้ายบนปุ่มเรนเดอร์เป็นฟอนต์คนละตัวกับทั้งหน้า (ในเทสต์เห็นเป็น
  กล่องสี่เหลี่ยม เพราะ fallback ไม่มี glyph ไทย)
- **สาเหตุจริง** — `ElevatedButton.styleFrom(textStyle: TextStyle(fontSize…))`
  **แทนที่** text style ทั้งก้อนของปุ่ม ไม่ได้ merge เข้ากับของเดิม ค่า
  `fontFamily` ที่ธีมให้มาจึงหายไป
- **วิธีแก้** — `62e7d13` ย้ายขนาด/น้ำหนักไปไว้ที่ `Text` ของปุ่มแทน
- **วิธีกันซ้ำ** — อย่าใส่ `textStyle:` ใน `styleFrom` ถ้าไม่ได้ระบุ
  `fontFamily` ด้วย — ใส่ที่ `Text` ปลอดภัยกว่า

## 2026-09-21 — `flutter build ios` ฟ้องว่าไม่รู้จักคลาสที่มีอยู่จริง

- **อาการ** — `flutter analyze` สะอาด `flutter test` ผ่านทั้งชุด แต่
  `flutter build ios` ล้มที่ `The method '_XxxSheet' isn't defined` ทั้งที่
  คลาสนั้นอยู่ในไฟล์เดียวกัน ระดับบนสุด วงเล็บครบ
- **สาเหตุจริง** — แคช kernel ของ build ค้างอยู่ที่สถานะกลางทาง (แก้ไฟล์เป็น
  สองจังหวะ: ใส่จุดเรียกใช้ก่อน แล้วค่อยเพิ่มตัวคลาสตามในอีกไม่กี่สิบวินาที)
- **วิธีแก้** — `rm -rf apps/user_app/.dart_tool/flutter_build` แล้วบิลด์ใหม่
- **วิธีกันซ้ำ** — analyze/test เขียวแต่ build ฟ้องชื่อที่ "มีอยู่แน่ ๆ" =
  แคช ไม่ใช่โค้ด ล้างแคชก่อนไล่หาสาเหตุอย่างอื่น

## 2026-09-21 — แก้โค้ดแล้วแต่บนจอยังเป็นของเก่า (คนละเครื่อง คนละบิลด์)

- **อาการ** — commit แล้ว บิลด์แล้ว แต่หน้าจอที่ดูอยู่ยังเป็นดีไซน์เดิม
  เสียเวลาไล่หาว่าโค้ดไม่เข้าเครื่องหรือเปล่า
- **สาเหตุจริง** — มีสองเครื่องที่รันแอปนี้ และคนละคำสั่งลงคนละเครื่อง:
  `flutter build ios --release` + `xcrun devicectl … install` → **ไอโฟนจริง**
  เท่านั้น ส่วน **ซิมูเลเตอร์** ต้อง `flutter build ios --simulator --debug`
  + `xcrun simctl install` แยกอีกชุด บิลด์ลงเครื่องหนึ่งไม่แตะอีกเครื่องเลย
- **วิธีกันซ้ำ** — ก่อนสรุปว่า "โค้ดไม่เข้า" ให้เทียบเวลา: เวลาไฟล์
  `Runner.app` (`stat -f "%Sm" …/build/ios/iphoneos/Runner.app`) กับเวลา
  commit ล่าสุด · และจำไว้ว่าสกรีนช็อตที่มีกรอบมือถือบนพื้นหลังเดสก์ท็อป
  = ซิมูเลเตอร์ ไม่ใช่เครื่องจริง

## 2026-09-21 — ข้อความในแถวโดนตัดกลางคำทั้งที่ยังมีที่ว่าง

- **อาการ** — `เกณฑ์การให้คะแนน` กลายเป็น `เกณฑ์การใ…` ทั้งที่ค่าทางขวาคือ
  `ไม่ใช้` สั้นนิดเดียว
- **สาเหตุจริง** — `Row` ที่มี `Flexible(flex: 1)` คู่กับ `Flexible(flex: 2)`
  แบ่งพื้นที่ตามสัดส่วนตายตัว ไม่สนว่าอีกฝั่งใช้จริงเท่าไร ฝั่งชื่อจึงได้แค่
  1 ใน 3 เสมอ
- **วิธีแก้** — `62e7d13` ให้ชื่อแถวกินตามความกว้างจริงผ่าน `LayoutBuilder` +
  `ConstrainedBox(maxWidth: c.maxWidth * 0.62)` แล้วค่าที่เหลือเป็น `Expanded`
  · ทางที่ยั่งยืนกว่าคือวางค่าไว้บรรทัดล่างของชื่อแถว (แบบที่หน้าใบงานใช้อยู่)
  ค่ายาวแค่ไหนก็ไม่แย่งที่กัน
- **วิธีกันซ้ำ** — `flex` คงที่เหมาะกับของที่กว้างพอ ๆ กันเท่านั้น ถ้าความยาว
  สองฝั่งไม่แน่นอน อย่าใช้สัดส่วนตายตัว

## 2026-09-21 — widget test หาแถวล่างของหน้าไม่เจอ หลังหน้ายาวขึ้น

- **อาการ** — เทสต์ที่เคยผ่านเริ่ม fail ว่า `Found 0 widgets with text …`
  หรือกดปุ่มแล้วไม่มีอะไรเกิดขึ้น หลังจากเพิ่มความสูงให้หน้า
- **สาเหตุจริง** — `ListView` สร้างเฉพาะลูกที่อยู่ในกรอบ (+cacheExtent) แถวที่
  หลุดจอใน viewport 844pt ของเทสต์จึงยังไม่ถูกสร้าง — `find` ไม่เจอ และการ
  `tap` ที่พิกัดนอกจอก็ไม่ถูกส่งถึง
- **วิธีกันซ้ำ** — ใน widget test ให้ `ensureVisible` หรือเลื่อน
  `Scrollable.position.jumpTo(maxScrollExtent)` ก่อนตรวจแถวท้ายหน้า
  (ผู้ใช้จริงก็ต้องเลื่อนเหมือนกัน — ไม่ใช่การลดสิ่งที่ตรวจ)

## 2026-09-21 — หลาย AI แก้ไฟล์เดียวกันพร้อมกัน

- **อาการ** — เปิดไฟล์มาเจอโค้ดที่ไม่มีใน git และไม่ใช่ของเรา (WIP ของอีก
  เซสชันที่ยังไม่ commit) ถ้าแก้ทับตรง ๆ งานของอีกฝั่งหายทันที
- **วิธีที่ใช้แล้วรอด** — `git worktree add` แยกสาขาของตัวเอง แล้ว **สแนปช็อต
  WIP ของเซสชันอื่นเป็น commit แรก** (`a3a7801`) ก่อนต่อยอด จากนั้นค่อย
  cherry-pick กลับเข้าเลนหลัก — ของทั้งสองฝั่งอยู่ครบและตามรอยได้
- **ผลข้างเคียงที่ต้องระวัง** — cherry-pick แบบนี้พางานของอีกเซสชันเข้ามาด้วย
  (ครั้งนี้คือ wizard 4 ขั้นของหน้าสร้างใบงาน ที่เจ้าของไม่ได้สั่งและต้องถอย
  ออกทีหลังใน `4c8037a`) — เวลารายงาน ต้องแยกให้ชัดว่าอันไหนงานเรา อันไหน
  ติดมาจากฐาน
