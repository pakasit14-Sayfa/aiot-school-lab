# ใบสั่งงาน — 3 บั๊กที่ยืนยันแล้วว่ามีอยู่จริง

> เขียน 2026-09-07 · ยืนยันกับโค้ดที่ commit `7055e9c` ทุกข้อ ไม่ได้ลอกมาจากรายงานของใคร
>
> **อ่าน `AGENTS.md` ก่อนเริ่ม** โดยเฉพาะหัวข้อ "เกณฑ์ว่าหน้าไหนเสร็จ"
> รันสถานะจริงด้วย `./scripts/state.sh`
>
> baseline ตอนรับงาน: `cd apps/user_app && flutter test` ต้องไม่เพิ่มจำนวน fail
> `flutter analyze` ต้องรัน **ทั้ง 3 แพ็กเกจ**: `packages/shared_core`,
> `packages/shared_ui`, `apps/user_app` — ตัวเดียวไม่ครอบอีกสองตัว

---

## 🔴 บั๊ก 1 — ปุ่มปิดเหตุฉุกเฉินบอกว่าสำเร็จทั้งที่ล้มเหลว

**ไฟล์:** `apps/user_app/lib/pages/executive_redesign_prototype/pages/director_emergency_page.dart`

หน้านี้มีปุ่มปิดเหตุ **3 จุด** และพฤติกรรมไม่เหมือนกันสักจุด

### 1.1 บรรทัด ~1840-1863 — กลืน error แล้วบอกสำเร็จ (ร้ายแรงสุด)

```dart
try {
  await IncidentService.closeIncidentReport(inc.id, ...);
} catch (e) {
  debugPrint('Error closing incident: $e');   // ← กลืน แล้วไหลต่อ
}
...
setState(() => sosResolved = true);            // ← อยู่นอก try ทำงานเสมอ
_showMessage('✓ ปิดเหตุการณ์ SOS เรียบร้อยแล้ว'); // ← บอกสำเร็จเสมอ
```

RPC ล้ม → ผู้อำนวยการเห็นว่าปิดเหตุแล้ว → ไม่มีใครตามต่อ **เหตุฉุกเฉินที่ยังเปิดอยู่
ถูกเข้าใจว่าปิดแล้ว** นี่คือเหตุผลที่จัดเป็นข้อร้ายแรงที่สุดในระบบ

### 1.2 บรรทัด ~4083-4133 และ ~4764 — หยุดถูก แต่โชว์ error ดิบ

```dart
content: Text('เกิดข้อผิดพลาด: $e'),
```

`$e` ขึ้นจอ ผิดเกณฑ์ข้อ "ไม่มี error ดิบ ($e) ขึ้นจอ" ใน `AGENTS.md`
ผู้ใช้เห็นข้อความอย่าง `PostgrestException(message: ...)` ซึ่งไม่ได้บอกว่าต้องทำอะไรต่อ

### 1.3 ทั้ง 3 จุดไม่มีการอ่านกลับมาตรวจ

RPC คืน 200 แต่แถวไม่เปลี่ยน (เช่นโดน role gate ปัดตกเงียบ ๆ) ก็ยังบอกสำเร็จ

### ต้องแก้ให้เป็น

รวมทั้ง 3 จุดให้ใช้ทางเดียวกัน:

1. `catch` ต้อง **หยุด** — ห้ามไหลไปตั้ง `sosResolved = true`
2. ข้อความบนจอเป็นภาษาคน เช่น `'ปิดเหตุไม่สำเร็จ เหตุการณ์ยังเปิดอยู่'`
   ส่วน `$e` ให้ `debugPrint` เท่านั้น
3. เขียนแล้ว **อ่าน canonical กลับ** (`IncidentService.listTeacherIncidentReports()`
   หรือ `getIncidentSummary()`) แล้วตรวจว่าเหตุนั้นเป็น `resolved`/`cancelled` จริง
   ถ้าไม่เจอ → โยน `backend_close_not_confirmed` และบอกผู้ใช้ว่าสถานะยังไม่เปลี่ยน
   **ค่อยบอกสำเร็จหลังจากยืนยันแล้วเท่านั้น**

ตัวอย่างที่ทำถูกแล้วในโปรเจกต์นี้ ลอกรูปแบบได้:
`director_reports_page.dart` เมธอด `_review()` — เขียน → อ่านกลับ → ตรวจ → ค่อยบอกสำเร็จ

### test ที่ต้องมี

`apps/user_app/test/executive/` — หน้านี้รับ seam ได้อยู่แล้ว (ดู
`director_emergency_page_test.dart` ที่มี `watchUpdates: false`) ต้องมีอย่างน้อย:

- RPC โยน exception → **ไม่มี** ข้อความ `'✓ ปิดเหตุ...'` บนจอ และ `sosResolved` ไม่เปลี่ยน
- RPC ผ่านแต่การอ่านกลับยังเห็นเหตุเปิดอยู่ → บอกว่าไม่สำเร็จ
- RPC ผ่านและอ่านกลับยืนยันแล้ว → บอกสำเร็จ
- ไม่มี `$e` โผล่บนจอในทุกกรณี

⚠️ `director_emergency_page_test.dart` ตอนนี้ fail อยู่ 2 ตัว **เป็นของเดิม** เป็นเทสต์
ที่ล้าสมัยตั้งแต่ก่อนหน้านี้ (ticket 1.3) แก้ไปด้วยได้ แต่อย่านับรวมว่าเป็นบั๊กใหม่

---

## 🟡 บั๊ก 2 — ไม่รู้สถานะ กลายเป็น "มาเรียน"

**ไฟล์:** `packages/shared_core/lib/models/parent_portal_model.dart` **บรรทัด 161**

```dart
status: row['status'] as String? ?? 'present',
```

backend ส่ง `status` เป็น null → ผู้ปกครองเห็นว่าลูก **"มาเรียน"** ทั้งที่ระบบไม่รู้
เป็นข้อมูลผิดแบบเงียบ ๆ ที่ไม่มีใครสังเกต

เทียบกับที่ทำถูกแล้วใน `staff_attendance_model.dart`: ไม่มีบันทึก = `no_record`
**ไม่ใช่** `present` เพราะ "ความเงียบไม่ใช่หลักฐานว่ามา"

### ต้องแก้ให้เป็น

- ทำ `status` เป็น nullable หรือใช้ค่า `'unknown'` — **ห้าม default เป็น `'present'`**
- ไล่หาทุกที่ที่ใช้ field นี้ (`grep -rn "attendance" apps/user_app/lib/pages/parent*`)
  แล้วแสดงว่า **"ไม่ทราบสถานะ"** ไม่ใช่ปล่อยให้ตกไปในช่อง "มาเรียน"
- ตัวนับสรุป (มาเรียนกี่วัน) ต้องไม่นับ unknown เป็นมาเรียน
- unit test ของ model: row ที่ไม่มี `status` ต้องไม่ออกมาเป็น present

---

## 🟠 บั๊ก 3 — 3 หน้าของผู้บริหารไม่มี backend เลย

ทั้ง 3 ไฟล์ **ไม่มี `import 'package:shared_core/shared_core.dart'` แม้แต่บรรทัดเดียว**
ข้อมูลบนจอเป็นค่าคงที่ในไฟล์ทั้งหมด

| ไฟล์ (`pages/executive_redesign_prototype/pages/`) | บรรทัด | backend ที่มีรออยู่ |
|---|---|---|
| `director_meetings_page.dart` | 2,215 | **มีครบแล้ว** — `20260907030000` + `040000` + `050000`: 9 ตาราง 31 RPC มี pgTAP แล้ว |
| `director_learning_page.dart` | 2,812 | ต้องสำรวจก่อน ดู `EXECUTIVE_BACKEND_SURVEY.md` |
| `director_scan_page.dart` | 1,081 | ต้องสำรวจก่อน — สแกนบัตรบุคลากรยังไม่มี RPC รองรับ |

### เริ่มที่ `director_meetings_page` ก่อน — backend พร้อมแล้ว

อ่าน **`docs/handoff/DECISIONS_2026-09-07.md`** ก่อนแตะหน้านี้ เจ้าของโปรเจกต์เคาะ
ข้อกำหนดไว้แล้ว 13 ข้อ **ห้ามถามซ้ำ ห้ามออกแบบใหม่เอง** ที่สำคัญ:

- การเรียกพบรายบุคคลไม่โผล่ในปฏิทินคนอื่น (school_admin เห็นได้)
- ครูปฏิเสธการเรียกพบไม่ได้ มีแค่ ตอบรับ / ขอเลื่อน (ต้องมีเหตุผล)
- บันทึกการประชุม: ร่าง → ปิด → **แก้ไม่ได้ เพิ่มต่อท้ายเท่านั้น**
- **"ไม่ต้องมีบันทึก" (`minutes_expected = false`) ต้องแสดงต่างจาก "ยังไม่ได้บันทึก"**
- เช็คชื่อผู้มาประชุม: ค่าเริ่มต้นคือ "ยังไม่ได้เช็คชื่อ" ไม่ใช่ "มาครบ"

**สิ่งที่ยังไม่มีและต้องทำเพิ่ม:** service/model ฝั่ง Dart ของระบบประชุม และ
Edge Function คู่ upload/download ของ bucket `meeting-files`
(ลอก `school-report-upload`/`-download` เป็นแม่แบบ — **อย่าลืม
`grant execute ... to service_role` ให้ RPC ที่ Edge Function เรียก** ไม่งั้นพังเงียบ
โปรเจกต์นี้เคยพลาดเรื่องนี้มาแล้ว 2 ครั้ง)

---

## กติกาที่พลาดบ่อยในโปรเจกต์นี้ — อ่านก่อนเริ่ม

1. **ไม่ใช่ Supabase Auth** — ทุก RPC รับ `p_token` เป็นอาร์กิวเมนต์แรก
   RPC ที่ไม่มี `p_token` เป็นของแอปอีกตัว เรียกแล้ว actor เป็น null เงียบ ๆ
2. **RLS deny-all ทุกตาราง** — `supabase.from('table').select()` คืนค่าว่างโดยไม่ error
   อ่าน/เขียนต้องผ่าน RPC เท่านั้น
3. **ลบข้อมูลปลอมแล้วต้องตามไปดูของที่เคยคู่กับมัน** — 2026-09-07 มี commit ที่ลบข้อมูล
   ปลอมออกถูกแล้ว แต่ลืมรายการตัวเลือกของตัวกรอง ทำให้เลือกแล้วรายการว่างเปล่าเงียบ ๆ
4. **`grep` พิสูจน์ได้แค่ "ไม่ต่อ backend แน่ ๆ" ไม่เคยพิสูจน์ว่า "ต่อครบแล้ว"** —
   ต้องอ่านทั้งไฟล์ก่อนสรุปว่าเสร็จ
5. **migration ที่ใช้ไปแล้ว:** ถึง `20260907060000` — ตัวถัดไปเริ่มที่ `20260907070000`
